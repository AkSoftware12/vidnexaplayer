name: videoplayer
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.24+24

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  # ✅ Required for localizationsDelegates (Material/Cupertino/Widgets in hi + en)
  flutter_localizations:
    sdk: flutter

  # ✅ Core Audio Stack
  # NOTE: just_audio_background was removed — audio_service already provides the
  # background/media-session layer and running both registers two competing
  # foreground services with incompatible MediaItem types.
  just_audio: ^0.10.4
  audio_service: ^0.18.18
  audio_session: ^0.2.2
  on_audio_query_forked: ^2.9.1
  screenshot: ^3.0.0
  gallery_saver_plus: ^3.2.9
  screen_brightness: ^2.1.7
  country_codes: ^3.1.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0






  #  # ✅ Optional Desktop Audio (if targeting Windows/Linux)
#  just_audio_mpv: ^0.1.7
#  audio_service_mpris: ^0.2.0

  # ✅ UI + Media
  fancy_bottom_navigation_2: ^0.3.5
  flutter_screenutil: ^5.9.3
  google_fonts: ^6.2.1
  flutter_svg: ^2.2.0
  fluttertoast: ^8.2.12
  flutter_colorpicker: ^1.1.0
  cached_network_image: ^3.4.1
  video_player: ^2.10.0
  video_thumbnail: ^0.5.6
  carousel_slider: ^5.1.1
  photo_manager: ^3.7.1
  image_picker: ^1.1.2

  # ✅ Navigation & Animation
  flutter_sequence_animation: ^4.0.0
  page_transition: ^2.2.1
  animations: ^2.0.11
  flutter_staggered_animations: ^1.1.1
  sliding_up_panel: ^2.0.0+1
  flutter_portal: ^1.1.4

  # ✅ Utility Packages
  file_picker: ^10.2.0
  shared_preferences: ^2.5.3
  package_info_plus: ^8.3.0
  permission_handler: ^12.0.1
  url_launcher: ^6.3.2
  path_provider: ^2.1.5
  provider: ^6.1.5
  intl: ^0.20.2
  # NOTE: `flutter_localization` was removed — it was never imported anywhere.
  # Localization now goes through `flutter_localizations` (declared above).
  flutter_tts: ^4.2.3
  firebase_messaging: ^15.2.9
  device_info_plus: ^11.5.0
  android_content_provider: ^0.5.2
  share_plus: ^11.0.0
  flutter_local_notifications: ^17.2.1
  # ✅ Was imported by Home/YoutubeScreen/playlists_screen.dart but never declared
  #    (it only resolved transitively — one upstream bump away from breaking).
  http: ^1.2.2
  # ✅ Salted SHA-256 hashing for the vault PIN (was stored in plaintext).
  crypto: ^3.0.3
  # ✅ Internet on/off detection for the connectivity banner.
  connectivity_plus: ^6.1.0

  # ✅ UI Helpers
  iconsax: ^0.0.8
  icons_plus: ^5.0.0
  marquee: ^2.3.0
  smooth_page_indicator: ^1.2.1
  text_scroll: ^0.2.0
  badges: ^3.1.2
  floating: ^6.0.0
  file_manager: ^1.0.2
  upgrader: ^11.5.0
  firebase_core: ^3.15.1
  new_version_plus: ^0.1.1
  image_cropper: ^8.0.2
  image: ^4.2.0
  media_scanner: ^2.2.0
  flutter_media_delete: ^1.0.1
  flutter_speed_dial: ^7.0.0
  font_awesome_flutter: ^10.9.1
  animate_do: ^4.2.0
  lottie: ^3.3.1
  path: ^1.9.0
  




  media_kit: ^1.2.2
  media_kit_video: ^2.0.0
  media_kit_libs_video: ^1.0.7
  volume_controller: ^3.4.0
  open_filex: ^4.4.0
#  disk_space_plus: ^0.2.6
  disk_space_plus: ^0.2.5
  rxdart: ^0.27.7
#  google_mobile_ads: ^7.0.0
  google_mobile_ads: ^6.0.0
  advanced_in_app_review: ^1.3.0
  android_intent_plus: ^6.0.0


  # whatsapp
  photo_view: ^0.14.0

  docman: ^1.2.0
  gal: ^1.9.1












dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.14.4
  # ✅ Build-time tooling only — was in `dependencies`, which shipped it in the app.
#  change_app_package_name: ^1.5.0

flutter_launcher_icons:
  android: "ic_launcher"
  ios: true
  image_path: "assets/appblue.png"
  min_sdk_android: 23
  web:
    generate: true
    image_path: "path/to/image.png"
    background_color: "#hexcode"
    theme_color: "#hexcode"
  windows:
    generate: true
    image_path: "path/to/image.png"
    icon_size: 48
  macos:
    generate: true
    image_path: "path/to/image.png"

flutter:
  # `generate: true` was set without an l10n.yaml or any .arb files, so it had
  # nothing to generate. Re-enable it together with a real l10n setup.
  uses-material-design: true
  assets:
    - assets/
    - assets/permisssion.json
    - assets/fonts/
  fonts:
    - family: PoppinsRegular
      fonts:
        - asset: assets/fonts/Poppins-Regular.ttf
    - family: PoppinsSemiBold
      fonts:
        - asset: assets/fonts/Poppins-SemiBold.ttf
    - family: PoppinsMedium
      fonts:
        - asset: assets/fonts/Poppins-Medium.ttf
    - family: PoppinsBold
      fonts:
        - asset: assets/fonts/Poppins-Bold.ttf
